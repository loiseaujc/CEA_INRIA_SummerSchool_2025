All the python packages used by these notebooks are listed in `requirements.txt`. The simplest is to create a dedicated `conda` environment (or any other virtual env) and run

```
pip install -r requirements.txt
```
